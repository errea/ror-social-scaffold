<section class="user-section">
  <h2>
    <%=  @user.name %></h2>
          
    <br></br>
   <%= button_display(@user) %>
  <% if current_user.id == @user.id || current_user.friends_and_own_posts%> 
  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
  <%end%>
</section>
